package com.example.fhir_subscription_challenge.controller;

public class PatientControllerTest {
    /*
    nothing here. I hardcoded the server uri directly in the controller.
    usually, one would retrieve the uri from a configuration
    this would have created a more boilerplate code which is why
    I decided against it.
     */
}
